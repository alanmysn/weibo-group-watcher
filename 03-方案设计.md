# 03 - 方案设计

> 项目：weibo-group-watcher（微博群聊镜像工具）
> 文档状态：已定稿（2026-08-27，用户逐项拍板后编制）
> 上一份：02-功能规划.md ｜ 下一份：04-开发计划.md

---

## 1. 总体架构：一个程序，三顶帽子

整个工具是**一个 Python 常驻进程**，内部三个模块像流水线一样单向协作：

```
微博服务器
   │  长连接 wss://web.im.weibo.com/im（实时推送，只收不发）
   ▼
┌────────────────── watcher 主进程（Python） ──────────────────┐
│                                                              │
│  [采集器 collector] ──→ [存储 storage] ──→ [面板服务 web]     │
│  收实时推送/断线补漏     写 SQLite 账本     读库 + SSE 广播    │
│                                              │               │
└──────────────────────────────────────────────┼───────────────┘
                                               ▼
                                浏览器面板（手机/电脑，只读）
```

要点：**面板从不直接与微博通信**。浏览器没开时采集照常入库；采集端临时断线时面板照样能翻历史。

## 2. 技术选型定案（2026-08-27 拍板）

| 决策项 | 定案 | 理由 |
|---|---|---|
| 语言 | **Python 3.11** | 机器环境现成；长连接库成熟；与 fund 项目同栈 |
| 进程形态 | **单进程**（采集/存储/面板三模块） | 部署、自启、排障只管一个东西 |
| 实时刷新 | **SSE**（服务器单向广播） | 浏览器原生支持；只读工具用不到双向通道 |
| 数据库 | **SQLite**（`data/watcher.db`） | 零安装、单文件、备份=复制文件；查询有索引 |
| 面板服务 | **Flask**（轻量 Web 框架） | 小而够用，路由/SSE/静态文件一把抓 |
| 前端 | **单文件 HTML+JS**（无构建、无框架） | 延续 habit-tracker 模式：好预览、好维护 |
| 开机自启 | **Windows 任务计划程序** | 系统自带；支持登录触发 + 崩溃自动重启（部署环节执行） |
| 主要依赖 | websocket-client（长连接）、requests（接口/图片）、Flask | 三件套，尽量少 |

## 3. 数据流（含异常路径）

| 场景 | 行为 |
|---|---|
| 正常在线 | 长连接收到推送 → 解析 → 入库 → SSE 喊面板刷新 |
| 断线/电脑关机后恢复 | 重连成功 → 缺口写入账本（见 §6.2），面板标注，**不自动补漏**（保住手机端角标）；用户择时手动补漏 |
| Cookie 失效 | 检测到（订阅被拒/接口 401）→ 状态置「钥匙过期」→ 面板顶栏提示 → 用户重扫码 → 热加载新钥匙继续 |
| 面板离线（没人开页面） | 采集与入库不受任何影响 |
| 面板打开但采集挂了 | 面板显示运行状态（§5 C7），历史照常可看 |

## 4. 数据库结构（SQLite，表草案）

| 表 | 关键字段 | 说明 |
|---|---|---|
| `messages` | `id`(微博消息ID，主键), `gid`, `from_uid`, `from_name`, `avatar_url`, `content`, `url_objects`, `type`, `media_type`, `media_data`, `time`, `recall_status`, `created_at` | 全部消息的唯一事实来源；`media_data` 只保留 fid/pic_infos 等下载字段，不复制整条原始包 |
| `images` | `msg_id`, `file_path`, `downloaded_at`, `size_bytes` | 消息↔图片文件对应表 |
| `attachments` | `msg_id`, `fid`, `file_name`, `extension`, `file_path`, `size_bytes`, `status`, `downloaded_at`, `error` | 聊天文件附件元数据；现阶段不缓存文件，`file_path`/`downloaded_at` 保留为空；不包含独立群文件库 |
| `read_state` | `last_read_msg_id`（单行） | 续读锚点：待读数的计算基准 |
| `special_users` | `uid`, `note` | 特别关注名单（C8 设置页维护） |
| `meta` | `key`, `value` | 杂项状态（运行状态、last_online_at、上次心跳等） |
| `gaps` | `start_ts`, `end_ts`（NULL=缺口进行中）, `dismissed_at`, `filled_at` | 永久缺口账本：区分首页提醒已关闭与补漏已完成，补漏后不删除记录 |

> 完整建表语句在实现阶段落码；此处为设计约定。已读判定 = 「消息已进入过用户屏幕视野」，面板上报，storage 记账。

> 存储原则（2026-08-31）：原始消息始终保存在单一 `watcher.db`，不按天拆库。后续 AI 整理从 SQLite 按日期、发言人 UID、消息类型查询，生成 `exports/` 下可删除、可再生成的 Markdown/JSONL/CSV。

## 5. 面板设计（对应 02 文档 C 组）

- **路由**：`/` 面板主页 ｜ `/api/messages?before=<id>` 向上翻页取历史 ｜ `/api/stream` SSE 实时流 ｜ `/api/state` 运行状态 ｜ `/settings` 特别关注设置 ｜ `/gaps` 最近 7 天遗漏记录
- **续读/特别角标**（R4/R5，2026-08-31 改版）：打开 → 底部最新并显示「待读 N」；点击跳第一条待读（分割线）并立即切换「★ 特别 N」；普通消息不再改变角标，点击特别角标依次跳到下一条特别消息并递减，归零消失；特别跳转跨过的普通消息一并计已读
- **特别关注消息**：整条高亮样式 + 左侧色条
- **搜索/按人筛选**（C5/C6）：顶部搜索框 + 点击发言人名筛选（走本地库查询）
- **运行状态**（C7）：顶栏常驻小灯：在线 / 重连中 / 钥匙过期
- **手机适配**：视口适配 + PWA manifest + 图标，浏览器「添加到主屏幕」即成 App 样子
- **Tailscale**：部署环节安装配置，外网手机安全回连家内面板

## 6. 采集器设计

### 6.1 长连接（探针 2026-08-27 实录配方 + 2026-08-29 帧序勘误）
1. WebSocket 连 `wss://web.im.weibo.com/im`，带 Cookie（`cookies.json` 加载），TLS 用项目内置 `ca_bundle.pem`（微博服务器不发中间证书）；
2. Bayeux 握手：`/meta/handshake` → 得 `clientId`；
3. 订阅私人频道：`/meta/subscribe` → `/im/{我的UID}`；
4. **激活（关键帧，缺此帧会话必在 ~170 秒后被 402）**：`/meta/connect` + `advice:{timeout:0}` → 回执后立即进入循环；
5. 普通 `/meta/connect` 循环：同一时刻仅一个未决 connect；服务器捏 ~170 秒 → 回复 → 立即发下一个；数据帧（`/im/{uid}` 频道）随到随处理；
6. 收到 `type:"groupchat"` 且 `gid` 匹配配置 → 解析入库；会话被踢/断线 → 指数退避自动重握手（2s 起、上限 60s）。

### 6.2 断线/停机补漏（A3 定案 + S4 定案；2026-08-29 实测勘误 + 08-30 策略改版）
- 实测要点：`query_messages.json` 页内为**时间正序**（旧→新）；**必须带 Referer 头**（缺失 10012 拒绝）；count=50 可用；**拉取本身会把服务器「已读位置」推到最新**（2026-08-30 对照实验实锤：轮询/补漏都会清掉手机端角标）；
- **策略改版（2026-08-30 用户拍板，方案 A）**：长连接**只收不拉**——启动不补漏、取消每小时对账，全程零自动拉取，保住手机端角标正常累计（对照实验：纯长连接收 57 条，手机角标 ≈57 一致）；
- 缺口处理：断线/停机/被 kill 的时段写入 `gaps` 永久账本（进程被杀由下次启动的「上次在线间隔 >120s」兜底认账）。首页黄色条永远只检查账本最新一条；关闭后写 `dismissed_at`，不回溯弹出更早缺口，刷新也不再出现，只有新缺口产生才再次提醒；
- 遗漏记录：`/gaps` 默认列出最近 7 天，补漏完成只写 `filled_at`，不删除历史。每条闭合且未补漏的记录提供「补漏至此」；点击较早缺口会依次处理它及其后更新的全部未补缺口；
- 双锚点有限补漏（2026-08-31）：对每个缺口取「缺口前最后一条本地消息」为停止锚点、「缺口后第一条本地消息」为首次 `max_mid`，只写入两锚点之间的消息。实测服务器允许任意已知旧消息 ID 作为首次 `max_mid`，返回内容严格早于该 ID；缺口后尚无本地消息时才从服务器最新页开始；
- 所有历史拉取均按**会清除手机端该群未读角标**处理，每次点击前明确二次确认；长连接继续零自动拉取；
- 三保险：每页间隔 0.5s / 单次最多扫描 2000 条服务器消息 / 未到停止锚点不标成功；已成功的较新缺口可单独标记，失败缺口保留未补状态；
- 库为空（首启/清空后）不回溯，从上线时刻开始积累。

### 6.3 Cookie 生命周期
- 明文存 `cookies.json`（gitignore）；失效检测见 §3；重扫码 = 重开实验浏览器档案登录 → 重新导出 → 进程热加载；
- 只读纪律：代码中**不存在任何发送类调用**，接口清单白名单制。

## 7. 图片与附件缓存（S3 定案；2026-08-31 接口实测）

- 文件名 = 消息 ID + 扩展名（如 `data/images/5336658524244983.jpg`）——天然去重、永不冲突；
- 类型实测：`media_type=1` 普通图片、`5` 文件附件、`10` 聊天视频、`15` GIF；普通图片/文件/视频封面均以 `fid` 指向微博文件服务，GIF 的 `pic_infos` 直接带图片地址；
- 下载配方：元数据 `upload.api.weibo.com/2/mss/meta_query.json`；文件内容 `upload.api.weibo.com/2/mss/msget`，图片加 `imageType=origin`。Cookie + Referer 实测可用；
- 下载时机：新图片/GIF/视频封面入库后双线程后台缓存；面板首次访问缺失图片缓存时可同步重试。临时文件写完后再原子改名，避免半文件；
- 面板 `<img>` 只指向本地文件路径，与 3 小时签名彻底无关；
- 图片/GIF/视频封面单文件上限 25 MB；文件附件不进入后台缓存；
- 清理（B3）：自动周期（默认 90 天，可配）+ 设置页「立即清理」按钮；删除文件同时清 `images` 表对应行（消息本体不动）。

### 7.1 聊天内文件附件（2026-08-31 纳入第 7 步）

- 能力边界：支持聊天消息中的文件附件下载，不等于独立「群文件库」；后者继续暂缓；
- 实测 `media_type=5` 为文件附件；历史样本经元数据接口正确取得 PDF 文件名、扩展名和大小，下载响应带文件名与总长度；
- 面板显示文件名/大小；用户点击下载后，工具才从微博读取并流式交给浏览器保存，不写入 `data/files/`。原始文件名用于显示和浏览器下载名，过滤路径、控制字符及 Windows 非法字符；
- 当前不主动回填旧媒体记录；新消息先验证稳定后，再决定是否单独执行一次历史媒体回填。

## 8. 撤回消息（S1/Q2 定案）

采集时已入库的消息若被撤回（推送 `recall_status`/332 事件），**保留原文，标注「已撤回」**；未及入库的撤回（仅收到事件）记一条「[一条消息已撤回]」占位。符合 R1 完整搬运精神。

## 9. 部署与运维

| 项 | 方案 |
|---|---|
| 启动 | 任务计划程序：用户登录时启动 `python main.py`，失败 1 分钟后自动重启（部署环节执行） |
| 启停开关 | 项目根目录三个双击脚本（2026-08-27 拍板）：`启动.bat`（拉起工具+确保自启启用）、`停止.bat`（结束进程+禁用自启，日常停用一步到位）、`状态.bat`（弹窗显示：是否运行/连接状态/库内消息条数） |
| 停止语义 | 停止 = 长连接断开，微博侧无副作用；数据与已读进度全保留；重新启动后把断档记入 `gaps` 账本并在面板标注，是否补回由用户按 §6.2 手动决定 |
| 面板边界 | 面板**不设**暂停/停止按钮（2026-08-27 拍板）：启停统一走脚本，避免面板引入状态机复杂度 |
| 彻底退役 | 停止 → 删除计划任务 → 删除项目文件夹即可，无隐藏服务无残留 |
| 日志 | `logs/watcher.log` 只记录启动、连接、重连、错误、入库条数、补漏结果等运行状态，不记录昵称或聊天正文；滚动保留 7 天 |
| 备份 | 复制 `data/watcher.db` 即全量备份（文档给出手动步骤） |
| 隐私 | `config.local.json`（群ID/群名）与 `cookies.json` 均已 gitignore，绝不入库 |

## 10. 风险与预案

| 风险 | 预案 |
|---|---|
| 微博改接口/长连接协议 | 失效表现明确（订阅被拒/无推送）→ 重新探针抄新配方（脚本已留存 Temp/weibo-probe） |
| 账号风控 | 只读 + 单连接 + 心跳温和 + 补漏限速；不做任何写操作 |
| Cookie 过期 | 面板显式提示 + 重扫流程（§6.3） |
| 磁盘膨胀 | 图片默认 90 天自动清理（§7）；消息库默认不清（B4 需手动开启+二次确认） |

## 11. 决策记录

| 日期 | 决策 |
|---|---|
| 2026-08-27 | S1 撤回=保留原文+标注；S2 选型六项（§2）；S3 图片缓存方案；S4 补漏翻页与三保险；单进程三帽架构；SQLite 表草案 |
| 2026-08-30 | 补漏方案 A：零自动拉取，缺口记账，用户手动补漏 |
| 2026-08-31 | 角标改为待读→特别双阶段；单一 SQLite + 可再生成 AI 导出；运行日志不含正文；聊天内文件附件纳入第 7 步 |
| 2026-08-31 | 缺口提醒改为首页只显最近一条且可永久关闭；新增最近 7 天遗漏记录页；补漏改为双锚点有限区间和「补漏至此」，账本补漏后保留 |
| 2026-08-31 | 实测媒体 1/5/10/15 与 meta_query/msget 配方；图片/GIF/视频封面本地缓存，文件附件点击后按需下载且不在工具内缓存，历史媒体暂不回填 |
