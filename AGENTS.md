# AGENTS.md — 项目施工守则

> 本文件给 AI 助手/协作者看：接手本项目先读这里，再按需读 01~04 文档。
> 此处只讲「这个项目怎么干活」；需求与产品背景见 01-需求记录.md。

## 项目一句话

微博群聊镜像工具：**只读**采集 1 个微博群的全部消息，本地 SQLite 存储，网页面板查看（手机可用）。协作模式：非技术用户任产品负责人，AI 任工程搭档。

## 文档体系（动工前按需读）

| 文档 | 内容 | 什么时候读 |
|---|---|---|
| 01-需求记录.md | R1-R6 需求、不做清单、存储清理策略 | 实现任何功能前 |
| 02-功能规划.md | MVP/暂缓/不做三张表（A/B/C/D 编号） | 判断范围与排期时 |
| 03-方案设计.md | 架构、选型、数据流、表结构、采集配方、运维 | 写代码前 |
| 04-开发计划.md | 11 步建造路线、每步验收标准、状态列 | 每一轮开工前 |

- 文档是活文档：每步实现后同步勾验收标准、更新状态列与「完成于」日期；结论性变更写入修订记录。
- 指路不抄书：文档间引用章节号，不复制正文。

## 红线（违反即事故）

1. **只读纪律（R6）**：代码中永远不得出现发送消息/点赞/关注等任何写操作的调用；接口按白名单使用。
2. **隐私纪律**：`cookies.json`、`config.local.json` 永不入库（已 gitignore）；任何文档、注释、日志、README 中不得出现群名/群 ID/用户 UID 明文。
3. **验收纪律**：每步必须用户亲手验证通过才算完成（04 文档验收栏）；AI 自测不替代验收。
4. **请示纪律**：任何文件编写/修改（文档或代码），动手前先请示用户、获确认后再动笔。
5. **UI 纪律**：未在讨论中出现过的界面元素、功能、提示文字一律不得擅自添加；先提议，拍板后动手。
6. **方案 A 红线（2026-08-30 用户拍板）**：补漏策略为**零自动拉取**（长连接只收不拉，缺口记 `gaps` 账本+面板标注，补漏仅由用户点「手动补漏」按钮触发）——**不得改回自动补漏/定时对账**（拉取会清掉用户手机端微博角标，实锤见 03 §6.2）。

## 协作与提交约定

- 流程：一轮一步（按 04 文档顺序），做 → 用户验收 → 提交 → 更新文档状态。
- Git：约定式前缀 + 一句话中文描述（`feat:` / `fix:` / `docs:` / `chore:`）；文档与代码分开提交；AI 只本地 commit，push 由用户在 GitHub Desktop 操作。
- 沟通：技术决策用大白话解释；重大取舍列选项 + 推荐，用户拍板后记录日期。

## 架构与配方速查

- 架构：单进程三帽（采集/存储/面板），数据单向流动 → 03 §1-§3；数据库六张表（messages/images/read_state/special_users/meta/gaps）→ 03 §4。
- 采集配方（2026-08-27 探针实测）：长连接 `wss://web.im.weibo.com/im`（Bayeux 握手 → 订阅 `/im/{uid}`）；历史消息接口 `webim/groupchat/query_messages.json`；握手帧与消息字段样例 → 03 §6.1。
- 配方失效预案：重新探针观摩网页版抄新配方（探针脚本曾留存于 `%TEMP%\weibo-probe`，临时目录可能被清理；方法与数据结构已固化在 03 §6）。
- 图片链接 3 小时整过期（实测）→ 必须本地缓存 → 03 §7。
- 补漏三保险：页间隔 0.5s / 单次上限 2000 条 / 超限面板标注，不静默丢弃 → 03 §6.2。
