# 02 - 功能规划

> 项目：weibo-group-watcher（微博群聊镜像工具）
> 文档状态：已定稿（2026-08-27，用户逐项拍板）
> 上一份：01-需求记录.md ｜ 下一份：03-方案设计.md

---

## 1. 规划方法

加法轮（列全候选，用户补漏：续读交互、清理设置、群文件疑问）→ 减法轮（砍成三张表）。每项标注确认日期；带 ⚠️ 的为留到方案设计再定细节的项。

## 2. MVP 清单（第一版就做）

### 采集侧（后台引擎）
| # | 功能 | 依据 |
|---|---|---|
| A1 | 长连接实时接收群消息 | R1/R2，探针已验证配方 |
| A2 | 断线自动重连 | 长连接必备 |
| A3 | **断线/停机补漏**：零自动拉取；缺口写入 `gaps` 永久账本。用户在遗漏记录页点「补漏至此」，系统以每个缺口前后本地消息为双锚点有限向前翻页，并累计处理所选及其后更新的未补缺口；所有历史拉取均按会清除手机端角标提示 | 2026-08-30 零自动拉取；2026-08-31 双锚点有限区间改版 |
| A4 | Cookie 失效检测 → 面板提醒重扫码 | 故障出口，必配 |
| A5 | 图片/GIF/视频封面自动缓存；聊天内文件附件识别并按需下载（实测类型 5，用户点击后才从微博文件服务读取） | Q3 + 用户 2026-08-31 确认网页聊天内存在文件附件 |

### 存储侧
| # | 功能 | 依据 |
|---|---|---|
| B1 | 消息库 SQLite 落盘（全部消息永不丢） | R1 资产本体 |
| B2 | 已读/待读状态记录（"看到即已读"账本） | R4 |
| B3 | 图片缓存清理设置：自动周期 + 立即清理按钮 | 用户 2026-08-27、08-31 拍板；附件不缓存，无需清理 |
| B4 | 消息库清理设置：**默认关闭** + 二次确认（清理即永久丢失；清空后无锚点，从清空时刻重新开始记录） | 用户 2026-08-27 拍板 |

### 面板侧（日常界面）
| # | 功能 | 依据 |
|---|---|---|
| C1 | 群消息流视图（文字/图片/链接/文件附件/系统通知正常显示） | R1/R3 |
| C2 | 向上滚动翻历史 | 用户对标微博客户端体验 |
| C3 | 「待读 N」角标 + 第一条待读定位 + 分割线；首次点击后退出普通计数模式 | R4 交互改版（2026-08-31） |
| C4 | 按 UID 设置特别关注并高亮；进入阅读后角标切换为「★ 特别 N」，依次跳转并递减；跨过的普通消息计已读 | R5 交互改版（2026-08-31） |
| C5 | 搜索聊天记录 + 按日期/发言人/消息类型导出 Markdown/JSONL/CSV | 用户 2026-08-27、08-31 拍板 |
| C6 | 按人筛选（点发言人名字查看其全部发言） | 同上 |
| C7 | 运行状态显示（在线/重连中/钥匙过期） | 可观测性 |
| C8 | 设置页（特别关注名单、清理设置） | B3/B4/C4 的配置入口 |
| C9 | 手机浏览器适配 + 添加主屏幕图标 | R3 |
| C10 | 遗漏管理：首页只提示最近一次未确认缺口，可永久关闭提醒；独立页面显示最近 7 天账本，每条提供「补漏至此」 | R7 |

### 运维侧
| # | 功能 | 依据 |
|---|---|---|
| D1 | 开机自启 | 长连接工具需常驻 |
| D2 | Tailscale 远程访问（外部手机看面板） | R3（延迟到部署环节装，不阻塞开发） |

## 3. 暂缓清单（第二版候选池）

| 功能 | 暂缓原因 / 恢复条件 |
|---|---|
| 独立群文件库 | 与聊天内文件附件不同；独立文件列表仍需专项观摩接口，聊天附件已进入 A5 |
| 外部推送渠道（微信/ntfy 等） | 已定不绑定微信；真实使用暴露"人不在面板前错过特别关注"的痛点时再做 |
| 搜索/筛选增强（命中高亮、日期范围、组合筛选） | 基础版先用，痛点驱动增强 |
| 图片缓存细粒度策略（只留最近 N 个月） | 先用全量周期清理，不够再细化 |

## 4. 不做清单（永久）

| 内容 | 理由 |
|---|---|
| 双向回复（面板/微信里回微博群） | 复杂度与风控翻倍；想回复点链接回微博 |
| 任何写操作（发送/点赞/关注） | 只读纪律，安全底线 |
| 多群支持 | 范围边界；架构留余地但不实现 |

## 5. 移交方案设计的问题

| # | 问题 |
|---|---|
| S1 | Q2 撤回消息呈现（倾向：保留原文+标注，⚠️ 待确认） |
| S2 | 技术选型定案（语言/数据库/面板服务形态/进程管理） |
| S3 | 图片缓存目录结构与文件命名（防重、防穿越） |
| S4 | 补漏的分页策略与单次补漏上限 |

## 6. 修订记录

| 日期 | 修订 |
|---|---|
| 2026-08-27 | 定稿；同日修订：取消首启历史打底（A4 并入 A3，只精准补漏），原 S1 撤销 |
| 2026-08-30 | 补漏改为方案 A：零自动拉取、缺口账本、用户手动补漏 |
| 2026-08-31 | 角标改为待读→特别双阶段；单一 SQLite + 可再生成导出；聊天内文件附件纳入第 7 步 |
| 2026-08-31 | 补漏升级为按缺口前后消息双锚点的有限区间；首页最近一条提醒 + 最近 7 天遗漏记录页；历史记录补漏后不删除 |
| 2026-08-31 | 第 7 步实测媒体类型与微博文件服务：图片 1、文件 5、视频 10、GIF 15；图片自动缓存上限 25 MB，文件附件改为点击后按需下载、不在工具内缓存 |
