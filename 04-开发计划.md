# 04 - 开发计划

> 项目：weibo-group-watcher（微博群聊镜像工具）
> 文档状态：已定稿（2026-08-27，用户拍板步骤切分与验收标准）
> 上一份：03-方案设计.md
>
> **执行纪律**：一轮一步；每步完成 = 用户亲手验收通过 + git 提交 + 本文档状态列更新。
> 验收栏是用户可亲手执行的检验动作，AI 模拟不算数。

---

## 步骤总览

| # | 步骤 | 覆盖功能（02 文档编号） | 状态 |
|---|---|---|---|
| 1 | 地基：骨架+配置+建表 | B1 前置 | ✅ 完成（2026-08-27） |
| 2 | 心跳时刻：长连接接入 | A1 A4 | ✅ 完成（2026-08-29，含两次重大修复，见下） |
| 3 | 韧性：重连+补漏 | A2 A3 | ✅ 完成（2026-08-29） |
| 4 | 面板初版：消息流+翻页+SSE | C1 C2 | ✅ 完成（2026-08-29，含四件套升级） |
| 5 | 续读：已读账本+角标+分割线 | B2 C3 | ✅ 已完成 |
| 6 | 特别关注与遗漏提醒 | C4 C10 | 🔄 特别关注初步验收通过；遗漏中心待验收 |
| 7 | 图片缓存与附件按需下载 | A5 B3(部分) | ✅ 完成（2026-08-31） |
| 8 | 检索与导出 | C5 C6 | ✅ 完成（2026-09-02） |
| 9 | 管家：设置页+清理 | C8 B3 B4 | ✅ 完成（2026-09-02） |
| 10 | 出门：手机适配+PWA+Tailscale | C9 D2 | ✅ 完成（2026-09-03） |
| 11 | 落户：启停脚本+自启+日志 | D1 + 运维 | ⬜ 未开始 |

---

## 各步详情

### 第 1 步 · 地基
**做**：项目骨架（main.py 入口）、`config.local.json` 加载、SQLite 初始六张表（第 7 步新增 `attachments`）、基础日志。
**验收**：运行初始化命令后，`data/watcher.db` 出现；日志输出「配置加载成功」。
**完成于**：2026-08-27

### 第 2 步 · 心跳时刻 ⭐
**做**：加载 Cookie → WebSocket 长连接 → Bayeux 握手订阅 → 解析 groupchat 推送 → 落库；运行日志只打印状态与入库条数，不记录昵称或正文。
**验收**：日志出现「收到并入库 1 条消息」；用户手机发一条，几秒内进入数据库和面板；重启进程消息不重复入库。
**注意**：若 Cookie 恰过期，此步需用户重新扫码。
**完成于**：2026-08-29

> **调试记录（2026-08-29）**：接入后遭遇"每 ~2 分 45 秒被服务器 402 踢掉"。两次误诊（客户端抢位说、心跳频率说）均被用户质疑后推翻；最终对比探针帧序找到真因——**Bayeux 会话需先发一帧 `connect+advice:{timeout:0}` 激活**，网页版的第③帧我们漏发了。补上后 402 彻底消失（观察期零复发，且越过历史死刑线 3 倍+）。附带修复：uv 环境证书链不全 + 微博服务器不发中间证书 → 项目内置 `ca_bundle.pem`；被踢/断网从崩溃改为指数退避自动复活。教训：**帧序对齐必须逐字节对照探针实录，不靠协议常识推断**。

### 第 3 步 · 韧性
**做**：断线自动重连；缺口账本与面板标注；用户手动触发补漏（后于第 6 步升级为按缺口双锚点有限翻页）；运行状态机（在线/重连中/钥匙过期）。长连接全程只收不拉，不做自动补漏或定时对账。
**验收**：真实断网实验——断网约 2 分钟（期间群内有新消息），恢复后确认工具自动重连、面板显示该时段缺口且未自动拉取；用户点击「手动补漏」后，逐条核对缺口消息补回且无重复。
**完成于**：2026-08-29

> **实验与调试记录（2026-08-29）**：① 停机实验（13:08~13:15 真实消息真空）抓出两 bug——翻页方向误设（实测接口页内为**时间正序**，见到锚点就停会漏掉其后的新消息）+ 连续性误设（长连接已抬走锚点时，洞会留在锚点之前）；最终方案为**自愈型对账**（窗口内每条幂等尝试入库，兼容任何位置的洞），实验缺口全部找回、零重复。② 直调历史接口 10012 被拒——变量矩阵实验定位缺 **Referer 头**；补上后 count=50 翻页正常。③ 运行状态机落 SQLite meta 表（跨进程可读）。④ 睡眠唤醒实验按用户确认省略（机制与已验证的停机实验同源）。⑤ 重启间隙实测：补漏自动找回 1 条漏网消息。

### 第 4 步 · 面板初版
**做**：Flask 服务；面板主页消息流（文字/链接/系统通知样式）；`/api/messages?before=` 向上翻页；`/api/stream` SSE 实时追加；新消息自动滚底；离开底部时显示“最新”按钮供一键返回。
**验收**：浏览器打开面板看到消息流；向上滚动加载更早消息；新消息不刷新自动长出。
**完成于**：2026-08-29

> **验收升级（用户 2026-08-29 提出，一并纳入）**：① 明亮主题（原深色改浅色）；② 说话人头像（懒缓存 `data/avatars/`，断网可看，首字兜底）；③ 表情渲染（`/webim/emotions.json` 映射 + 懒缓存 `data/emotions/`）；④ 微博分享渲染成卡片（标题/作者/转评赞/链接，`url_objects` 富字段采集与历史回填）；⑤ 视频分享先给 📹 占位标记，封面随第 7 步图片模块接入。存储配套：messages 表新增 `avatar_url`、`url_objects` 列（老库自动补列），采集/补漏均幂等刷新富字段。

> **补漏策略改版（2026-08-30 当时方案，范围算法已由下方 08-31 方案替代）**：用户发现「工具获取消息后手机微博角标消失」。对照实验定案：**拉取接口会把服务器「已读位置」推到最新**（last_read_mid 实测推进），任何自动拉取（启动补漏/每小时对账）都会周期性清掉手机端角标；而**纯长连接只收不拉时手机角标正常累计**（实验：纯长连接收 57 条 vs 手机角标 ≈57 一致）。据此改版：① 取消启动补漏与每小时对账（全程零自动拉取）；② 断线/停机/被 kill 时段记入 `gaps` 账本（进程被杀由启动时「上次在线间隔 >120s」兜底认账），面板黄色标注条显示「⏸ 工具休眠 XX:XX ~ XX:XX，期间消息未采集」；③ 面板新增「手动补漏」按钮（防重入，复用自愈型对账，完成后闭合缺口标注消失；会清一次手机角标，故只由用户择时触发）；④ 补漏只覆盖锚点之后，锚点之前的更早历史洞维持现状不补（用户拍板）。

> **补漏范围再改版（2026-08-31，09-02 调整提醒）**：旧方案从服务器最新消息翻到库内最新锚点，无法可靠处理锚点之前积累的多个历史缺口。实测确认历史接口允许任意已知消息 ID 作为首次 `max_mid`，且只返回更早消息。新方案以每个缺口前后两条本地消息作双锚点有限翻页；首页不主动提示，`/gaps` 保留最近 7 天可视账本，每条提供「补漏至此」，点击较早缺口累计处理它及其后未补缺口。补漏后记录保留并标为已补漏；所有历史拉取均提示会清除手机端该群未读角标。

### 第 5 步 · 续读
**做**：已读上报接口（进入视野即记）；`read_state` 锚点；待读角标；分割线；点角标跳最早待读处。
**验收**：关面板 → 群内来 3 条 → 重开面板角标显示 3 → 点角标跳到分割线；角标后续行为已在第 6 步升级为「特别关注」阶段。
**实现状态**：✅ 用户验收通过（2026-08-30）：`/api/read` 上报、待读角标、第一条待读定位、分割线、滚动即已读、只前进不倒退锚点；首页打开后定位最新消息，程序自动定位不误记已读。
**完成于**：2026-08-30

### 第 6 步 · 特别关注与遗漏提醒
**做**：`special_users` 按 UID 匹配（改名不失效）；设置页星标名单；消息高亮；首次点击「待读 N」后角标立即切换「★ 特别 N」；点击依次跳转并递减，跨过的普通消息计已读。缺口仅后台记录，首页不主动提示；保留最近 7 天遗漏记录页和双锚点「补漏至此」。
**验收**：① 改名单后高亮生效；关面板积累普通+特别消息 → 重开显示待读 → 首次点击跳第一条待读并切换特别角标 → 连续点击依次跳转、递减、归零消失；无特别消息时首次跳转后角标消失。② 制造两个缺口 → 首页不出现提示；遗漏记录页保留两条。点击较新的「补漏至此」只补较新缺口；点击较早的会连同其后未补缺口处理；确认框明确提示清除手机端未读角标；成功后记录仍在且显示已补漏。
**实现状态**：🔄 特别关注初步验证无问题；遗漏管理代码已实现，等待真实缺口验收。
**完成于**：⬜

### 第 7 步 · 图片与附件缓存
**做**：保留 `fid`/`pic_infos` 媒体字段；图片、GIF、视频封面异步下载、去重、原子落盘和本地显示；文件附件只读取名称/大小，用户点击后才从微博流式下载到浏览器，工具内不缓存；文件名净化；图片上限 25 MB；不扩展到独立群文件库。
**验收**：群里发图 → `data/images/` 出现对应文件并可离线查看；群里发送文件附件 → 面板显示文件名/大小，未点击时工具内无附件文件，点击后浏览器正常下载且危险文件名被净化。
**实现状态**：✅ 用户验收通过（2026-08-31）：图片/GIF/视频封面自动缓存并在面板显示；文件附件显示名称/大小，点击后由浏览器按需下载，工具内不缓存附件。现有历史媒体暂不回填。
**完成于**：⬜

### 第 8 步 · 检索与导出
**做**：搜索框（关键词查询）；点发言人名筛选；按日期/发言人 UID/消息类型从 SQLite 导出 Markdown、JSONL 或 CSV；导出文件写入 `exports/`，可删除重建。
**验收**：关键词和按人筛选结果正确；指定一天或发言人导出后内容正确；选择「仅用户发言」时不含系统通知；删除导出文件不影响原始数据库。
**实现状态**：✅ 搜索、按人筛选及导出均由用户验收通过；导出支持日期、发言人 UID、内容类型及 Markdown/JSONL/CSV。
**完成于**：2026-09-02

### 第 9 步 · 管家
**做**：独立 `/storage` 集中图片与消息清理（特别名单仍在 `/settings`）；图片超过 90 天自动清理，手动可选全部或超过 1/2/3 个月；消息不自动清理，手动可选保留最近 1/2/3 个月或删除全部，执行前预览数量并二次确认。消息清理同步处理关联图片、附件信息及被删时段的遗漏账本。
**验收**：图片立即清理后旧图消失、新图保留；消息清理预览数量正确，取消确认不删除，确认后保留期内消息仍在、对应旧缺口与关联缓存消失；特别关注名单与运行设置不变。
**实现状态**：✅ 用户验收通过；独立存储管理页、图片自动/手动清理、消息清理预览与确认、关联数据同步清理均已完成。
**完成于**：2026-09-02

### 第 10 步 · 出门
**做**：手机视口适配；PWA manifest + 图标；Tailscale 安装配置（外部访问）。
**验收**：手机浏览器打开面板 → 添加到主屏幕 → 桌面图标点开全屏使用；手机在外部网络经 Tailscale 打开面板。
**实现状态**：✅ 用户验收通过：安卓手机排版与导航正常，可添加到主屏幕；Tailscale Serve 以私网 HTTPS 代理本机 `127.0.0.1:8765`，电脑与手机同一 tailnet 时可访问，未启用 Funnel。
**完成于**：2026-09-03

### 第 11 步 · 落户
**做**：`启动.bat` / `停止.bat` / `状态.bat` 三脚本；任务计划程序自启（登录触发+失败重启）；日志滚动保留 7 天。
**验收**：重启电脑 → 状态脚本显示在线；双击停止脚本 → 进程消失、自启禁用；再双击启动脚本恢复。
**完成于**：⬜

---

## 全局约定

- **只读纪律**：任何步骤不得引入发送类调用（R6）；
- **隐私纪律**：`cookies.json` / `config.local.json` 永不入库；
- **验收纪律**：每步用户亲手验证后才提交、才算完成；本文档状态列同步更新；
- **暂停语义**：任何时刻停掉工具都不丢数据（03 文档 §9）；
- 里程碑后用户可随时喊停迭代——真实使用数天后的痛点进第 5 阶段（迭代打磨）。
