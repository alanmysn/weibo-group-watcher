# 04 - 开发计划

> 项目：weibo-group-watcher（微博群聊镜像工具）
> 文档状态：已定稿（2026-08-27，用户拍板步骤切分与验收标准）
> 上一份：03-方案设计.md
>
> **执行纪律**：一轮一步；每步完成 = 用户亲手验收通过 + git 提交 + 本文档状态列更新。
> 验收栏是用户可亲手执行的检验动作，AI 模拟不算数。

---

## 步骤总览

| # | 步骤 | 覆盖功能（02 文档编号） | 状态 |
|---|---|---|---|
| 1 | 地基：骨架+配置+建表 | B1 前置 | ⬜ 未开始 |
| 2 | 心跳时刻：长连接接入 | A1 A4 | ⬜ 未开始 |
| 3 | 韧性：重连+补漏 | A2 A3 | ⬜ 未开始 |
| 4 | 面板初版：消息流+翻页+SSE | C1 C2 | ⬜ 未开始 |
| 5 | 续读：已读账本+角标+分割线 | B2 C3 | ⬜ 未开始 |
| 6 | 特别关注 | C4 | ⬜ 未开始 |
| 7 | 图片缓存 | A5 B3(部分) | ⬜ 未开始 |
| 8 | 检索：搜索+按人筛选 | C5 C6 | ⬜ 未开始 |
| 9 | 管家：设置页+清理 | C8 B3 B4 | ⬜ 未开始 |
| 10 | 出门：手机适配+PWA+Tailscale | C9 D2 | ⬜ 未开始 |
| 11 | 落户：启停脚本+自启+日志 | D1 + 运维 | ⬜ 未开始 |

---

## 各步详情

### 第 1 步 · 地基
**做**：项目骨架（main.py 入口）、`config.local.json` 加载、SQLite 建表（03 文档 §4 五张表）、基础日志。
**验收**：运行初始化命令后，`data/watcher.db` 出现；日志输出「配置加载成功」。
**完成于**：⬜

### 第 2 步 · 心跳时刻 ⭐
**做**：加载 Cookie → WebSocket 长连接 → Bayeux 握手订阅 → 解析 groupchat 推送 → 落库；命令行实时打印新消息。
**验收**：命令行滚动「收到新消息：……」；用户手机发一条，几秒内出现在屏幕上；重启进程消息不重复入库。
**注意**：若 Cookie 恰过期，此步需用户重新扫码。
**完成于**：⬜

### 第 3 步 · 韧性
**做**：断线自动重连；锚点补漏（query_messages 翻页推进 + 三保险：页间隔 0.5s / 单次上限 2000 / 超限标注）；运行状态机（在线/重连中/钥匙过期）。
**验收**：真实断网实验——断网约 2 分钟（期间群内有新消息），恢复后日志显示「补漏 N 条」，逐条核对面板无缺口、无重复。
**完成于**：⬜

### 第 4 步 · 面板初版
**做**：Flask 服务；面板主页消息流（文字/链接/系统通知样式）；`/api/messages?before=` 向上翻页；`/api/stream` SSE 实时追加；新消息自动滚底。
**验收**：浏览器打开面板看到消息流；向上滚动加载更早消息；新消息不刷新自动长出。
**完成于**：⬜

### 第 5 步 · 续读
**做**：已读上报接口（进入视野即记）；`read_state` 锚点；待读角标；分割线；点角标跳最早待读处。
**验收**：关面板 → 群内来 3 条 → 重开面板角标显示 3 → 点角标跳到分割线 → 向上翻阅角标同步递减。
**完成于**：⬜

### 第 6 步 · 特别关注
**做**：`special_users` 名单匹配（按 UID）；高亮样式；待读清零后角标转「发送人 +n」；点击跳最早一条未读特别消息。
**验收**：名单用户发言整条高亮；清零后角标变形；点击跳转位置正确。
**完成于**：⬜

### 第 7 步 · 图片缓存
**做**：含图消息异步下载（消息 ID 命名）；去重跳过；失败重试 3 次；面板显示本地图片。
**验收**：群里发图 → `data/images/` 出现对应文件 → 面板正常显示；**断网后**面板图片仍能看（证明走本地缓存）。
**完成于**：⬜

### 第 8 步 · 检索
**做**：搜索框（关键词查询）；点发言人名筛选其全部发言；结果分页。
**验收**：输关键词结果正确且秒出；点人名只显示其发言。
**完成于**：⬜

### 第 9 步 · 管家
**做**：`/settings` 设置页（特别名单编辑、清理配置）；图片自动周期清理（默认 90 天）+ 立即清理按钮；消息库清理（默认关闭 + 二次确认 + 「从清空时刻重新记录」警示文案）。
**验收**：改名单立即生效；立即清理后旧图消失、新图保留；消息库清理有二次确认且确认文案含警示。
**完成于**：⬜

### 第 10 步 · 出门
**做**：手机视口适配；PWA manifest + 图标；Tailscale 安装配置（外部访问）。
**验收**：手机浏览器打开面板 → 添加到主屏幕 → 桌面图标点开全屏使用；手机在外部网络经 Tailscale 打开面板。
**完成于**：⬜

### 第 11 步 · 落户
**做**：`启动.bat` / `停止.bat` / `状态.bat` 三脚本；任务计划程序自启（登录触发+失败重启）；日志滚动保留 7 天。
**验收**：重启电脑 → 状态脚本显示在线；双击停止脚本 → 进程消失、自启禁用；再双击启动脚本恢复。
**完成于**：⬜

---

## 全局约定

- **只读纪律**：任何步骤不得引入发送类调用（R6）；
- **隐私纪律**：`cookies.json` / `config.local.json` 永不入库；
- **验收纪律**：每步用户亲手验证后才提交、才算完成；本文档状态列同步更新；
- **暂停语义**：任何时刻停掉工具都不丢数据（03 文档 §9）；
- 里程碑后用户可随时喊停迭代——真实使用数天后的痛点进第 5 阶段（迭代打磨）。
